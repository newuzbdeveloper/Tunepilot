import { styled } from "styled-components";

export const ContentWrapper = styled.main`
  padding: 0 120px;
`;

function HomePage() {
  return (
    <ContentWrapper>
      <div>hello</div>
    </ContentWrapper>
  );
}

export default HomePage;
